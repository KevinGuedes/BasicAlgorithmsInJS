//Algoritmo que conta quantos valores inteiros existem entre 1 (inclusive) e N (inclusive).

var number = 125;
var count = 0;
for(var i = 1; i <= number; i++){
    count++
}

//Apresentação dos resultados
console.log(`Quantidade de valores inteiros entre 1 e ${number} é ${count}`);
